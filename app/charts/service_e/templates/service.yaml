apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_e.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "service_e.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_e.selectorLabels" . | nindent 4 }}
