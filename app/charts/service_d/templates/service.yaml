apiVersion: v1
kind: Service
metadata:
  name: {{ include "service_d.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "service_d.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service_d.selectorLabels" . | nindent 4 }}
