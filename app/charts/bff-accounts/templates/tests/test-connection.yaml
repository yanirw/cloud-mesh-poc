apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bff-accounts.fullname" . }}-test-connection"
  labels:
    {{- include "bff-accounts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bff-accounts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
